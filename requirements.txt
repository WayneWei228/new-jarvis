# Jarvis — Proactive AI Assistant

# LLM (AWS Bedrock)
anthropic>=0.40.0

# Camera capture
opencv-python>=4.9.0

# Audio capture & processing
sounddevice>=0.4.6
numpy>=1.26.0

# Xunfei (iFlytek) real-time ASR
websocket-client>=1.6.0

# macOS native overlay (NSPanel + WKWebView)
pyobjc-framework-Cocoa>=10.0
pyobjc-framework-WebKit>=10.0

# Async HTTP
aiohttp>=3.9.0
